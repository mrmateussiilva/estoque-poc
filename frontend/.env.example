# Environment Variables

## Frontend (.env)

```
VITE_API_BASE_URL=http://localhost:8080
```

Para produção, altere para o domínio da API:
```
VITE_API_BASE_URL=https://api.sge.com
```

O Vite requer o prefixo `VITE_` para expor variáveis ao frontend.
