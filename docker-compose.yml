services:
  backend:
    build: .
    container_name: sge-backend
    ports:
      - "8003:8003"
    volumes:
      - sge-data:/root
    environment:
      - PORT=8003
    restart: unless-stopped

volumes:
  sge-data:
