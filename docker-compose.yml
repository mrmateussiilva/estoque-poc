services:
  backend:
    build: .
    container_name: sge-backend
    ports:
      - "8003:8003"
    env_file:
      - .env
    environment:
      - PORT=8003
      - ALLOWED_ORIGIN=*
    restart: unless-stopped
