# S.G.E. - Environment Configuration Example

# -- APP CONFIG --
ENV=production
DEBUG=false
PORT=8003
JWT_SECRET=seu-segredo-de-pelo-menos-32-caracteres-aqui

# -- DATABASE CONFIG --
# Você pode usar DATABASE_URL ou as variáveis separadas
# DATABASE_URL=root:pass@tcp(localhost:3306)/estoque?parseTime=true&loc=Local
DB_USER=root
DB_PASS=root
DB_HOST=localhost
DB_PORT=3306
DB_NAME=estoque

# -- WORKER POOLS --
NFE_WORKERS=5
EXPORT_WORKERS=3
EXPORT_DIR=./exports

# -- LOGGING --
LOG_FORMAT=text # ou json para produção com Graylog/Cloudwatch

# -- FRONTEND --
# Se o frontend for servido pelo backend (padrão), não precisa de VITE_API_URL
# Caso use dev server do Vite separadamente:
VITE_API_URL=http://localhost:8003
